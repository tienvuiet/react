export type Data = {
    // value: number,
    counter:any
}
export type Student = {
    id: number, 
    name: string
}
export type InitialStudentState = {
    students: Student[]
}
export type User = {
    id: number, 
    name: string ,
    love: boolean
}